# ---------------------------------------------------------------------------
# boardgames-rag — local environment secrets
#
# Week 1 (ingestion) does not require any of these. They land later:
#   - GEMINI_API_KEY: free-tier Gemini 2.0 Flash, used as the Ragas judge
#     in week 4. https://aistudio.google.com/app/apikey
#   - GROQ_API_KEY:   free-tier hosted inference for the agentic loop in
#     week 5. https://console.groq.com/keys
#
# Copy this file to `.env` and fill in values when needed. `.env` is gitignored.
# ---------------------------------------------------------------------------

GEMINI_API_KEY=
GROQ_API_KEY=
